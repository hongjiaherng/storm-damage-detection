name: storm-damage-detection-cpu
channels:
  - pytorch
  - defaults # default channel's prioritized over conda-forge
  - conda-forge
dependencies:
  - defaults::python=3.11.*
  - defaults::pip
  - pytorch::pytorch=2.2.*
  - pytorch::cpuonly # TODO: comment this if you are using a Mac with >= M1 chip (torch supports M1 GPU natively with metal backend)
  - conda-forge::gdal
  - conda-forge::xarray
  - pip:
    # Packages for accessing Landsat & Sentinel-2
    - rasterio
    - rioxarray
    - xarray-spatial
    - pystac-client
    - planetary-computer
    - odc-stac
    - odc-algo
    # Packages for YOLO
    - ultralytics
    - opencv-python-headless
    - labelme2yolo
    # Additional packages
    - torchvision==0.17.*
    - torchaudio==2.2.*
    - gdown
    - huggingface_hub
    - python-dotenv
    - ipykernel     # TODO: jupyter kernel for notebook/lab, comment it out if you are not planning to use them 